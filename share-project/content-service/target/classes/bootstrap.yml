spring:
  application:
    name: content-drt
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
        namespace: test
        group: DEFAULT_GROUP
        timeout: 5000
        config-long-poll-timeout: 1000
        config-retry-time: 2000
        max-retry: 3
        refresh-enabled: true
#        extension-configs:
#          -dataId:
#           group:
#           refresh: true

